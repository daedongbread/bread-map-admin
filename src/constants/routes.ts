enum Routes {
  LOGIN = '/login',
  BAKERIES = '/bakeries',
  BAKERY_REPORT = '/bakery-report',
  USERS = '/users',
  USER_REPORT = '/user-report',
}

enum GhRoutes {
  LOGIN = '/bread-map-admin/login',
  BAKERIES = '/bread-map-admin/bakeries',
  BAKERY_REPORT = '/bread-map-admin/bakery-report',
  USERS = '/bread-map-admin/users',
  USER_REPORT = '/bread-map-admin/user-report',
}

export { Routes, GhRoutes };
